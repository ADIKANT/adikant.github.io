# Active Context

## Current Slice

The executive redesign has been implemented:

- page structure rebuilt around hero -> value -> impact -> cases -> leadership -> model -> domains -> tech -> speaking -> timeline -> best fit -> contact;
- editable copy moved into `content.js`;
- unsafe screenshots removed from the repo;
- safe SVG preview assets and updated README added.

## Working Assumptions

- Current portrait stays in the hero, but not as the social preview image.
- `30+` is the standardized team adoption metric across the page.
- Browser-rendered smoke testing is still limited by the local environment because no JS runtime or headless browser is installed.
